# Pulse — Raspberry Pi Requirements
# Run: pip install -r requirements_pi.txt --break-system-packages

pyserial        # serial communication with Arduino
RPLCD           # LCD display control
smbus2          # I2C communication for LCD
opencv-python   # camera input and frame processing
sounddevice     # microphone audio recording
scipy           # save audio as WAV file for Whisper
requests        # send audio/data to laptop server

# --- NOT pip installable ---

# piper (text to speech):
# pip install piper-tts --break-system-packages
# download voice: wget https://huggingface.co/rhasspy/piper-voices/resolve/main/en/en_US/amy/medium/en_US-amy-medium.onnx
# download config: wget https://huggingface.co/rhasspy/piper-voices/resolve/main/en/en_US/amy/medium/en_US-amy-medium.onnx.json

# i2c-tools (for LCD I2C):
# sudo apt-get install i2c-tools -y

# python dev headers (required for ydlidar SDK build):
# sudo apt-get install python3-dev -y

# cmake (required for ydlidar SDK build):
# sudo apt-get install cmake -y

# swig (required for ydlidar SDK Python bindings):
# sudo apt-get install swig -y

# ydlidar SDK (LiDAR driver):
# git clone https://github.com/YDLIDAR/YDLidar-SDK.git
# cd YDLidar-SDK && pip install .

# threading, time, json, os, subprocess, tempfile — Python standard library