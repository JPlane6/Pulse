requests          # Send HTTP requests to Mac Flask server
opencv-python     # Camera capture + image processing
pyserial          # Serial communication with Arduino
numpy             # Array/math backbone (used by OpenCV anyway)