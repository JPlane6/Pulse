# Recommended to use Python 3.12

opencv-python      # computer vision library — handles camera input and drawing bounding boxes
torch              # machine learning framework — powers YOLOv8 inference, enables M2 GPU (MPS) acceleration
torchvision        # torch extension — provides image processing utilities used by YOLO
torchaudio         # torch extension — audio processing utilities, needed for future TTS integration on Pi
ultralytics        # YOLOv8 — object detection model that identifies what's in each camera frame
