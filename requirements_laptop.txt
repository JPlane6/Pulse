# Pulse — Laptop (Mac) Requirements
# Run: pip install -r requirements_laptop.txt

opencv-python   # camera input and drawing bounding boxes
torch           # ML framework — powers YOLOv8, M2 GPU acceleration
torchvision     # torch image processing utilities
torchaudio      # torch audio utilities
ultralytics     # YOLOv8 object detection
openai-whisper  # speech to text — runs on M2 GPU
sounddevice     # microphone audio (for local testing only)
scipy           # save audio as WAV for Whisper
flask           # web server — receives requests from Pi
requests        # send requests to Ollama

# --- NOT pip installable ---

# Ollama (local AI — phi3:mini for questions, llava for vision):
# curl -fsSL https://ollama.com/install.sh | sh
# ollama pull phi3:mini
# ollama pull llava

# ffmpeg (required by Whisper):
# brew install ffmpeg

# threading, time, json, os, tempfile — Python standard library